# 当前开发状态

正式项目骨架已经创建在 `left-deng-seat-app`。

已具备：
- Next.js 目录结构
- mock 数据和类型定义
- 登录、注册、确认密码、用户名重复校验
- 自证提交、动态自证项、打回原因展示
- 用户端座位登记、重复座位提醒、附近可见范围
- 管理端用户审核、打回、注销、改座位
- 管理端统计、全区座位示意图、场馆区域配置、自证配置
- 座位可见范围函数和座位重复校验函数
- Supabase client 占位
- 注册 / 登录 / 退出 / 当前用户 API
- 用户保存座位 API
- 管理员通过 / 打回 / 注销 / 改座位 API
- 自证项读取 / 用户自证提交 / 管理员自证配置 API
- 自证截图上传和管理员临时查看 API
- 管理员统计 / 全区座位图 API
- 用户附近同担安全可见范围 API
- 密码哈希和登录 cookie 工具
- 数据库 schema 草案和 RLS 草案
- Supabase 设置文档

本轮已完成：
- 把 HTML 原型核心交互拆成 React 组件
- 将主页面接入 Auth / Proof / User / Admin 四块功能
- 将表单提交改成客户端安全写法，方便后面接真实数据库

还没有完成：
- 已写入本地 Supabase 环境变量，等待执行数据库初始化 SQL
- 当前环境无法执行 npm install / 启动开发服务，所以暂未做真实编译运行
- 图片上传接口已接 Supabase Storage，前端页面还未切换到真实接口

下一步：
- 增加 Supabase 数据访问层
- 把 mock 数据替换成真实数据库读写
- 根据 Supabase 项目信息配置环境变量后再启动本地测试


本轮新增：
- 本地 Supabase 环境变量已写入 `.env.local`
- 登录 / 注册 / 退出 / 读取当前用户已切到真实 API
- 用户提交自证会上传图片到 Supabase Storage，再写入数据库
- 用户保存座位已切到真实 API
- 管理端用户列表会读取真实用户、自证答案和临时截图链接
- 管理端通过 / 打回 / 注销 / 改座位 / 自证项增删会写真实数据库

待完成：
- 管理端统计页和全区座位图前端仍需从真实统计接口读取
- 用户端附近同担页面仍需从 `/api/me/nearby` 读取真实附近数据
- 场馆配置前端仍需接新增 / 修改 / 删除区域接口
- 当前环境仍无法执行 npm install / next dev，所以还未真实编译运行


本轮继续完成：
- 用户端附近同担页面已从真实接口读取，服务端限制同区、前后 5 排、左右 10 号
- 用户留言保存已写入真实数据库
- 管理端统计页已读取真实统计接口
- 管理端全区座位图已读取真实座位图接口
- 场馆配置已支持新增、修改、删除区域
- 场馆配置支持逐排座位数，例如 `14,14,16`

现在剩余：
- 在可执行 npm 的环境里安装依赖并启动编译测试
- 根据测试结果修小问题
- 配置 Vercel 环境变量并部署
